const QUOTE_SCHEMA={type:'object',properties:{cliente_id:{type:['string','null']},cliente_texto:{type:'string'},ubicacion:{type:'string'},duracion:{type:'string'},modalidad_costo:{type:'string'},precio_total_explicito:{type:['number','null']},currency:{type:'string'},cantidad_global:{type:'number'},partidas:{type:'array',items:{type:'object',properties:{tipo:{type:'string'},nombre:{type:'string'},producto_id:{type:['string','null']},servicio_id:{type:['string','null']},cantidad:{type:'number'},unidad:{type:'string'},precio_unitario:{type:'number'},alcance_precio:{type:'string'},descripcion:{type:'string'}},required:['tipo','nombre','producto_id','servicio_id','cantidad','unidad','precio_unitario','alcance_precio','descripcion']}},confianza:{type:'number'},ambiguedades:{type:'array',items:{type:'string'}}},required:['cliente_id','cliente_texto','ubicacion','duracion','modalidad_costo','precio_total_explicito','currency','cantidad_global','partidas','confianza','ambiguedades']};
const jsonClean=v=>{if(typeof v!=='string')return v;let s=v.trim().replace(/^```(?:json)?/i,'').replace(/```$/,'').trim();try{return JSON.parse(s)}catch{return null}};
const normalizeQuote=j=>{const x=j||{};return{cliente_id:x.cliente_id||null,cliente_texto:String(x.cliente_texto||''),ubicacion:String(x.ubicacion||''),duracion:String(x.duracion||''),modalidad_costo:String(x.modalidad_costo||'DESGLOSADO').toUpperCase(),precio_total_explicito:x.precio_total_explicito==null?null:Number(x.precio_total_explicito)||0,currency:String(x.currency||'PEN'),cantidad_global:Number(x.cantidad_global||1),partidas:Array.isArray(x.partidas)?x.partidas.map(p=>({tipo:String(p.tipo||'SERVICIO').toUpperCase(),nombre:String(p.nombre||'').trim(),producto_id:p.producto_id||null,servicio_id:p.servicio_id||null,cantidad:Number(p.cantidad||1),unidad:String(p.unidad||'UND'),precio_unitario:Number(p.precio_unitario||0),alcance_precio:String(p.alcance_precio||'global').toLowerCase(),descripcion:String(p.descripcion||'')})).filter(p=>p.nombre&&p.precio_unitario>=0):[],confianza:Math.max(0,Math.min(1,Number(x.confianza||0))),ambiguedades:Array.isArray(x.ambiguedades)?x.ambiguedades.map(String):[]}};
const quotePrompt=(text,ctx)=>`Eres el motor semántico de M.A.R.C., un sistema de cotizaciones. Interpreta el dictado completo, no palabras aisladas. Devuelve SOLO JSON válido según el esquema solicitado. NO inventes datos. La fuente de verdad es el dictado y el catálogo/clientes suministrados.\n\nREGLAS: 1) Extrae cliente, ubicación, duración, modalidad y partidas. 2) Para cantidades/precios: solo multiplica un precio por la cantidad cuando el dictado diga cada, por unidad, por cámara, por equipo, por pieza, por metro, por hora o equivalente. Si dice solo “materiales 80 soles” o “instalación 300”, trátalo como precio GLOBAL. 3) “TODO COSTO” solo si el usuario lo dice o lo expresa inequívocamente; entonces no inventes materiales/productos y usa una partida global. 4) Si hay precio total explícito y partidas detalladas, conserva ambas; no uses el total para alterar las partidas. 5) No inventes IDs: producto_id/servicio_id deben ser IDs del contexto o null. 6) Si no puedes decidir, agrega una ambigüedad. 7) No confundas ubicación con cliente. 8) Los números con punto pueden ser miles: 13.500 = 13500.\n\nDICTADO:\n${String(text).slice(0,12000)}\n\nCLIENTES DISPONIBLES:\n${JSON.stringify(ctx?.clientes||[]).slice(0,30000)}\n\nPRODUCTOS DISPONIBLES:\n${JSON.stringify(ctx?.productos||[]).slice(0,30000)}\n\nSERVICIOS DISPONIBLES:\n${JSON.stringify(ctx?.servicios||[]).slice(0,20000)}`;
export default{async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/analyze-catalog-page'){if(request.method!=='POST')return Response.json({error:'Método no permitido'},{status:405});try{const body=await request.json();const image=String(body?.image||'');const tipo=String(body?.tipo||'PROVEEDOR');const pagina=Number(body?.pagina||0);const ocr=String(body?.ocr_text||'');const filas=Array.isArray(body?.filas_tabla)?body.filas_tabla:[];if(!image)return Response.json({error:'Falta la imagen de la página'},{status:400});const re=/\b(?:AD|PRO)-[A-Z0-9]+(?:-[A-Z0-9]+)*\b/ig;const allowed=[...new Set([...filas.map(x=>String(x?.codigo||'').trim().toUpperCase()),...String(ocr.match(re)||[]).map(x=>x.toUpperCase())].filter(x=>/^(AD|PRO)-[A-Z0-9-]+$/.test(x)))];const prompt=`Eres el lector visual de un catálogo comercial. Analiza toda la página. No inventes nombres, códigos ni precios. Cada fila/tarjeta con código propio es un producto o variante. ${allowed.length?'Códigos detectados: '+allowed.join(', '):'Lee todos los códigos AD-/PRO- visibles'}. Devuelve SOLO JSON {items:[...]}. Tipo: ${tipo}. Página: ${pagina}. OCR: ${ocr.slice(0,14000)}`;const schema={type:'object',properties:{items:{type:'array',items:{type:'object',properties:{codigo:{type:'string'},nombre:{type:'string'},marca:{type:'string'},modelo:{type:'string'},categoria:{type:'string'},descripcion:{type:'string'},precio_fuente:{type:'number'},unidad:{type:'string'},unidad_precio:{type:'string'},cantidad_paquete:{type:'number'},presentacion:{type:'string'},variante:{type:'string'}},required:['codigo','nombre','marca','modelo','categoria','descripcion','precio_fuente','unidad','unidad_precio','cantidad_paquete','presentacion','variante']}},required:['items']};const providers=[];if(env.GEMINI_API_KEY)providers.push(async()=>{const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent?key='+encodeURIComponent(env.GEMINI_API_KEY),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{role:'user',parts:[{text:prompt},{inline_data:{mime_type:'image/jpeg',data:image.split(',').pop()}}]}],generationConfig:{responseMimeType:'application/json',responseSchema:schema,maxOutputTokens:8000}})});const j=await r.json();if(!r.ok)throw Error(j?.error?.message||'Gemini no disponible');return j?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('')||'{}'});if(env.AI)providers.push(async()=>{const r=await env.AI.run('@cf/google/gemma-4-26b-a4b-it',{prompt,image:image.split(',').pop()});return typeof r==='string'?r:JSON.stringify(r)});for(const p of providers){try{return Response.json({items:JSON.parse(await p()),pagina,tipo,fuente:'IA visual'})}catch(e){}}return Response.json({items:[],error:'Ningún motor visual disponible'},{status:503})}catch(e){return Response.json({error:String(e?.message||e)},{status:500})}}
if(url.pathname==='/api/transcribe-quote'){if(request.method!=='POST')return Response.json({error:'Método no permitido'},{status:405});try{const b=await request.json();if(!b.audio)throw Error('Falta audio');if(!env.AI)throw Error('Workers AI no está disponible');const r=await env.AI.run('@cf/openai/whisper-large-v3-turbo',{audio:String(b.audio),task:'transcribe',language:'es',vad_filter:true,initial_prompt:'Cotizaciones empresariales en español peruano: clientes, bloques, cámaras IP, materiales, instalación, soles, precios, cantidades, ubicación.'});return Response.json({text:String(r?.text||'')})}catch(e){return Response.json({error:String(e?.message||e)},{status:500})}}
if(url.pathname==='/api/interpret-quote'){if(request.method!=='POST')return Response.json({error:'Método no permitido'},{status:405});try{const b=await request.json();const prompt=quotePrompt(b.text,b.context||{});if(env.AI){const r=await env.AI.run('@cf/meta/llama-3.3-70b-instruct-fp8-fast',{messages:[{role:'system',content:'Devuelve únicamente JSON válido. No escribas explicaciones.'},{role:'user',content:prompt}]});const raw=r?.response??r?.result??r;const parsed=jsonClean(raw);if(parsed)return Response.json({quote:normalizeQuote(parsed),modelo:'@cf/meta/llama-3.3-70b-instruct-fp8-fast'})}if(env.GEMINI_API_KEY){const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent?key='+encodeURIComponent(env.GEMINI_API_KEY),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{role:'user',parts:[{text:prompt}]}],generationConfig:{responseMimeType:'application/json',responseSchema:QUOTE_SCHEMA,maxOutputTokens:5000}})});const j=await r.json();const raw=j?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('')||'';const parsed=jsonClean(raw);if(parsed)return Response.json({quote:normalizeQuote(parsed),modelo:'gemini-3.6-flash'})}throw Error('No hay motor de interpretación disponible')}catch(e){return Response.json({error:String(e?.message||e)},{status:503})}}
if(env.ASSETS&&typeof env.ASSETS.fetch==='function')return env.ASSETS.fetch(request);return new Response('M.A.R.C. Worker OK',{status:200})}};