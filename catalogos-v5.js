/* M.A.R.C. — Motor V5.7: filas de tabla como fuente de verdad + IA para enriquecer */
(()=>{
'use strict';
const BUCKET='catalog-pdfs';let running=false;
const clean=v=>String(v??'').replace(/\s+/g,' ').trim();
const num=v=>{let s=String(v??'').replace(/[^0-9.,-]/g,'');if(s.includes(',')&&s.includes('.'))s=s.replace(/\./g,'').replace(',','.');else s=s.replace(',','.');const n=Number(s);return Number.isFinite(n)?n:0};
const cat=s=>{s=clean(s).toUpperCase();if(/AUDIFONO|AURICULAR|EARPHONE/.test(s))return'Audífonos';if(/CARGADOR/.test(s))return'Cargadores';if(/CABLE/.test(s))return'Cables';if(/MOUSE|TECLADO/.test(s))return'Periféricos';if(/SOPORTE/.test(s))return'Soportes';if(/ADAPTADOR|ADAPTER/.test(s))return'Adaptadores';if(/MICROFONO|MICRÓFONO/.test(s))return'Micrófonos';if(/PARLANTE|SPEAKER/.test(s))return'Parlantes';if(/MEMORIA|USB/.test(s))return'Memorias';if(/WATCH|RELOJ/.test(s))return'Smartwatch';if(/POWER BANK/.test(s))return'Power banks';return'Accesorios'};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const codeRe=/\b(?:AD|PRO)(?:-[A-Z0-9]+)+\b/ig;const codeOk=s=>/^(?:AD|PRO)-[A-Z0-9-]+$/i.test(clean(s));
function codeTokens(s){const a=[];String(s||'').replace(codeRe,m=>{a.push(m.toUpperCase());return m});return [...new Set(a)]}
function validName(name,code=''){const n=clean(name).replace(/^[-–—|:]+|[-–—|:]+$/g,'');if(n.length<3||!/[A-Za-zÁÉÍÓÚÑáéíóúñ]/.test(n))return false;if(/^(PRECIO|CÓDIGO|CODIGO|FOTO|NOMBRE|UNIDAD|DESCRIPCIÓN)$/i.test(n))return false;if(/^S\.?\/?\s*[0-9]/i.test(n))return false;if(code&&n.replace(new RegExp(code,'ig'),'').trim().length<2)return false;return true}
function normalizeName(s,code){let n=clean(s);if(code)n=n.replace(new RegExp(code,'ig'),' ');n=n.replace(/\b(?:PRECIO|DOCENA|CÓDIGO|CODIGO|FOTO|NOMBRE)\b/ig,' ');return clean(n)}
async function nativeRows(page){const tc=await page.getTextContent({normalizeWhitespace:false,disableCombineTextItems:false});const arr=(tc.items||[]).map(it=>{const t=clean(it.str);const tr=it.transform||[];return t?{t,x:Number(tr[4]||0),y:Number(tr[5]||0),w:Number(it.width||0)}:null}).filter(Boolean);const groups=[];for(const it of arr){let g=groups.find(z=>Math.abs(z.y-it.y)<=8);if(!g){g={y:it.y,items:[]};groups.push(g)}g.items.push(it)}return groups.sort((a,b)=>b.y-a.y).map(g=>{const items=g.items.sort((a,b)=>a.x-b.x),text=clean(items.map(x=>x.t).join(' ')),codes=codeTokens(text);let price=0;for(let i=items.length-1;i>=0;i--){const ms=items[i].t.match(/(?:S\.?\/\.?\s*)?(\d{1,5}(?:[.,]\d{1,2}))(?!\d)/g)||[];if(ms.length){const p=num(ms[ms.length-1]);if(p>0&&p<100000){price=p;break}}}let name='';if(codes.length){const idx=items.findIndex(x=>codeOk(x.t));const before=idx>=0?items.slice(0,idx).map(x=>x.t).join(' '):'';name=normalizeName(before,codes[0]);if(!validName(name,codes[0])){const after=idx>=0?items.slice(idx+1).map(x=>x.t).join(' '):'';name=normalizeName(after,codes[0])}}return{text,codes,price,name,y:g.y}}).filter(r=>r.codes.length&&r.price>0&&validName(r.name,r.codes[0]))}
function candidatesFromRows(rows,type,page){const out=[];for(const r of rows){for(const code of r.codes){if(!codeOk(code)||!validName(r.name,code)||r.price<=0)continue;out.push({codigo:code,nombre:r.name,marca:'',modelo:code,categoria:cat(r.name),descripcion:r.name,precio_compra:type==='PROVEEDOR'?r.price:0,precio_venta:type==='PROPIO'?r.price:0,unidad:'UND',datos_extra:{fuente:'PDF texto nativo',pagina:page,tipo:type,precio_fuente:r.price,cantidad_paquete:1,presentacion:'UND',variante:'',confianza:'Fila de tabla PDF'}})}return out}
function similarity(a,b){a=clean(a).toUpperCase();b=clean(b).toUpperCase();if(!a||!b)return 0;if(a===b)return 1;if(a.includes(b)||b.includes(a))return .85;const A=new Set(a.split(/\s+/)),B=new Set(b.split(/\s+/));let n=0;A.forEach(x=>{if(B.has(x))n++});return n/Math.max(A.size,B.size,1)}
async function ai(image,type,page,ocrText,candidates){const table=candidates.map(x=>({codigo:x.codigo,nombre:x.nombre,precio:x.datos_extra.precio_fuente}));const r=await fetch('/api/analyze-catalog-page',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image,tipo:type,pagina:page,ocr_text:String(ocrText||'').slice(0,12000),filas_tabla:table})});const j=await r.json().catch(()=>({}));if(!r.ok||!Array.isArray(j.items))throw new Error(j.error||'IA no disponible');const out=candidates.map(c=>({...c}));for(const x of j.items){const code=clean(x.codigo).toUpperCase();const matches=out.map((c,i)=>({c,i,score:c.codigo===code?1:0.0+similarity(x.nombre,c.nombre)*.5})).filter(z=>z.c.codigo===code).sort((a,b)=>b.score-a.score);const m=matches[0];if(!m||m.score<.5)continue;const p=num(x.precio_fuente);const c=m.c;c.nombre=validName(x.nombre,code)?clean(x.nombre):c.nombre;c.marca=clean(x.marca)||c.marca;c.modelo=clean(x.modelo)||c.modelo;c.categoria=clean(x.categoria)||c.categoria;c.descripcion=clean(x.descripcion)||c.descripcion;c.datos_extra={...c.datos_extra,fuente:'IA visual + fila PDF',confianza:'IA visual validada',cantidad_paquete:Math.max(1,Number(x.cantidad_paquete||c.datos_extra.cantidad_paquete||1)),presentacion:clean(x.presentacion)||c.datos_extra.presentacion,variante:clean(x.variante||''),unidad_precio:clean(x.unidad_precio||'')};if(p>0&&Math.abs(p-c.datos_extra.precio_fuente)/c.datos_extra.precio_fuente<.25){c.datos_extra.precio_fuente=p;c.precio_compra=type==='PROVEEDOR'?p:c.precio_compra;c.precio_venta=type==='PROPIO'?p:c.precio_venta}c.unidad=clean(x.unidad)||c.unidad}return out}
async function save(items,id,uid,seen){const fresh=items.filter(x=>{const k=x.codigo.toUpperCase()+'|'+clean(x.nombre).toUpperCase();if(seen.has(k))return false;seen.add(k);return true});if(!fresh.length)return 0;const rows=fresh.map(x=>({user_id:uid,catalogo_id:id,codigo:x.codigo,nombre:x.nombre,marca:x.marca||null,modelo:x.modelo||null,categoria:x.categoria||'Accesorios',descripcion:x.descripcion||null,precio_compra:Number(x.precio_compra||0),precio_venta:Number(x.precio_venta||0),unidad:x.unidad||'UND',datos_extra:x.datos_extra||{}}));const r=await supabaseClient.from('catalogo_items').insert(rows);if(r.error)throw r.error;return rows.length}
function status(t){const e=document.querySelector('#cat4Progress');if(e)e.textContent=t}
async function checkpoint(id,page,count,total,state='PROCESANDO',err=null){const r=await supabaseClient.from('catalogos_pdf').update({paginas:total,paginas_procesadas:page,bloques_total:total,bloque_actual:page,productos_encontrados:count,estado:state,error_detalle:err}).eq('id',id);if(r.error)throw r.error}
async function processFile(file,id,type){const uid=(await user()).id,p=await pdfjs(),pdf=await p.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise;let total=0;const seen=new Set();await checkpoint(id,0,0,pdf.numPages);for(let n=1;n<=pdf.numPages;n++){status(`V5.7 · página ${n}/${pdf.numPages} · leyendo filas reales · ${total}`);const page=await pdf.getPage(n);let rows=[];try{rows=await nativeRows(page)}catch(e){console.warn('Filas nativas',n,e)}let candidates=candidatesFromRows(rows,type,n);let raw='',image='';try{image=await render(page);const o=await ocr(image);raw=o}catch(e){console.warn('OCR página',n,e)}if(candidates.length&&image){try{candidates=await ai(image,type,n,raw,candidates)}catch(e){console.warn('IA enriquecimiento página',n,e)}}if(!candidates.length&&raw){const codes=new Set(codeTokens(raw));const h=heuristic(raw,type,n,codes);candidates=h}if(candidates.length)total+=await save(candidates,id,uid,seen);await checkpoint(id,n,total,pdf.numPages,'PROCESANDO');page.cleanup?.()}await checkpoint(id,pdf.numPages,total,pdf.numPages,'PROCESADO',total?'':'No se encontraron filas válidas');status(`✓ V5.7 terminado · ${total} productos/variantes únicos. Revisa antes de inventario.`);await list()}
function heuristic(raw,type,page,allowed){const lines=String(raw||'').split(/\r?\n/).map(clean).filter(Boolean),out=[];for(let i=0;i<lines.length;i++){for(const code of codeTokens(lines[i])){if(!allowed.has(code))continue;const block=lines.slice(i,Math.min(lines.length,i+5));let price=0;for(const z of block){const ms=z.match(/(?:S\.?\/\.?\s*)?(\d{1,5}(?:[.,]\d{1,2}))/g)||[];for(const q of ms){const p=num(q);if(p>0&&p<100000)price=p}}let name=normalizeName(lines[i],code);if(!validName(name,code))for(const z of block.slice(1,3)){if(validName(z,code)){name=z;break}}if(price&&validName(name,code))out.push({codigo:code,nombre:name,marca:'',modelo:code,categoria:cat(name),descripcion:name,precio_compra:type==='PROVEEDOR'?price:0,precio_venta:type==='PROPIO'?price:0,unidad:'UND',datos_extra:{fuente:'OCR respaldo',pagina:page,tipo:type,precio_fuente:price,cantidad_paquete:1,presentacion:'UND',confianza:'OCR respaldo'}})}}return out}
async function user(){const r=await supabaseClient.auth.getUser();if(r.error||!r.data.user)throw new Error('Tu sesión expiró.');return r.data.user}
async function pdfjs(){if(window.pdfjsLib)return window.pdfjsLib;const p=await import('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs');p.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs';window.pdfjsLib=p;return p}
async function render(page){const b=page.getViewport({scale:1}),scale=Math.min(2.8,Math.max(2,2800/Math.max(b.width,1))),v=page.getViewport({scale}),c=document.createElement('canvas');c.width=Math.ceil(v.width);c.height=Math.ceil(v.height);await page.render({canvasContext:c.getContext('2d',{alpha:false}),viewport:v}).promise;const d=c.toDataURL('image/jpeg',.94);c.width=1;c.height=1;return d}
async function nativeText(page){const tc=await page.getTextContent({normalizeWhitespace:true});return(tc.items||[]).map(x=>String(x.str||'').trim()).filter(Boolean).join('\n')}
async function ocr(image){if(!window.Tesseract)await new Promise((ok,no)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js';s.onload=ok;s.onerror=()=>no(new Error('No se pudo cargar el OCR local.'));document.head.appendChild(s)});const w=await window.Tesseract.createWorker('spa');try{const r=await w.recognize(image);return String(r?.data?.text||'')}finally{await w.terminate()}}
async function upload(file,type){if(running)return;running=true;try{if(!file||(!/\.pdf$/i.test(file.name)&&file.type!=='application/pdf'))throw new Error('Selecciona un PDF.');if(file.size>95*1024*1024)throw new Error('El PDF supera 95 MB.');const uid=(await user()).id;status('V5.7 · subiendo PDF…');const path=`${uid}/${crypto.randomUUID()}.pdf`;let r=await supabaseClient.storage.from(BUCKET).upload(path,file,{contentType:'application/pdf',upsert:false});if(r.error)throw r.error;r=await supabaseClient.from('catalogos_pdf').insert({user_id:uid,nombre:file.name,tipo,archivo_path:path,tamano_bytes:file.size,estado:'PROCESANDO',paginas_procesadas:0,bloques_total:0,bloque_actual:0,productos_encontrados:0,error_detalle:null}).select('id').single();if(r.error){await supabaseClient.storage.from(BUCKET).remove([path]);throw r.error}await processFile(file,r.data.id,type)}catch(e){console.error(e);status(`Error V5.7: ${e.message||e}`);alert(`No se pudo procesar el catálogo.\n\n${e.message||e}`)}finally{running=false}}
async function reprocess(id,button){if(running)return;running=true;if(button){button.disabled=true;button.textContent='Procesando…'}try{const uid=(await user()).id,r=await supabaseClient.from('catalogos_pdf').select('tipo,archivo_path').eq('id',id).eq('user_id',uid).single();if(r.error)throw r.error;const d=await supabaseClient.storage.from(BUCKET).download(r.data.archivo_path);if(d.error)throw d.error;const del=await supabaseClient.from('catalogo_items').delete().eq('catalogo_id',id).eq('user_id',uid);if(del.error)throw del.error;await processFile(new File([d.data],`catalogo-${id}.pdf`,{type:'application/pdf'}),id,r.data.tipo)}catch(e){console.error(e);status(`Error V5.7: ${e.message||e}`);alert(`No se pudo reprocesar el catálogo.\n\n${e.message||e}`)}finally{running=false;if(button){button.disabled=false;button.textContent='Releer catálogo'}}}
async function list(){const w=document.querySelector('#cat4List');if(!w)return;try{const u=await user(),r=await supabaseClient.from('catalogos_pdf').select('id,nombre,tipo,paginas,estado,paginas_procesadas,productos_encontrados,error_detalle').eq('user_id',u.id).order('created_at',{ascending:false});if(r.error)throw r.error;w.innerHTML=(r.data||[]).map(c=>{const s=String(c.estado||'').toUpperCase();return`<article class="cat4-row"><div class="cat4-file">PDF</div><div class="cat4-main"><b>${esc(c.nombre)}</b><span>${c.tipo==='PROVEEDOR'?'Proveedor · precio compra':'Mi catálogo · precio venta'} · ${c.paginas||'?'} páginas</span><small>${s==='PROCESADO'?'✓ Procesado':s==='PROCESANDO'?`⏳ Página ${c.paginas_procesadas||0}/${c.paginas||'?'} `:`⚠ ${esc(c.error_detalle||'Error')}`} · ${c.productos_encontrados||0} productos/variantes únicos</small></div>${s==='PROCESADO'?`<button class="btn btn-secondary cat4-v5" data-id="${c.id}">Releer catálogo</button>`:`<button class="btn btn-secondary cat4-resume" data-id="${c.id}">${s==='ERROR'?'Reintentar':'Continuar'}</button>`}<button class="cat4-delete" data-id="${c.id}">×</button></article>`}).join('')||'<div class="cat4-empty"><b>📚 No hay catálogos</b><span>Selecciona un PDF para importar productos.</span></div>';w.querySelectorAll('.cat4-v5,.cat4-resume').forEach(b=>b.onclick=()=>reprocess(b.dataset.id,b));w.querySelectorAll('.cat4-delete').forEach(b=>b.onclick=async()=>{if(!confirm('¿Eliminar este catálogo y sus productos detectados?'))return;const u=await user(),x=await supabaseClient.from('catalogos_pdf').select('archivo_path').eq('id',b.dataset.id).single();if(x.data?.archivo_path)await supabaseClient.storage.from(BUCKET).remove([x.data.archivo_path]);await supabaseClient.from('catalogo_items').delete().eq('catalogo_id',b.dataset.id).eq('user_id',u.id);await supabaseClient.from('catalogos_pdf').delete().eq('id',b.dataset.id).eq('user_id',u.id);await list()})}catch(e){w.innerHTML=`<div class="cat4-empty">${esc(e.message)}</div>`}}
function hookInputs(){const own=document.querySelector('#cat4Own'),prov=document.querySelector('#cat4Prov');const bind=(el,type)=>{if(!el||el.dataset.v57)return;el.dataset.v57='1';el.addEventListener('change',e=>{e.stopImmediatePropagation();const f=e.target.files?.[0];if(f){window.__MARC_CATALOG_TYPE=type;upload(f,type)}},true)};bind(own,'PROPIO');bind(prov,'PROVEEDOR')}
const start=()=>{hookInputs();new MutationObserver(()=>hookInputs()).observe(document.body,{childList:true,subtree:true});setTimeout(list,600)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else setTimeout(start,250);window.MARC_REPROCESS_CATALOG=reprocess;window.MARC_V57_UPLOAD=upload;
})();
