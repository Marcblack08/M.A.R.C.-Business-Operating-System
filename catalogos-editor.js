/* M.A.R.C. — Editor previo a inventario */
(()=>{
'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function uid(){const r=await supabaseClient.auth.getUser();if(r.error||!r.data.user)throw new Error('Tu sesión expiró.');return r.data.user.id}
let current=null,items=[];
const fields=[['codigo','Código / SKU'],['nombre','Nombre del producto'],['marca','Marca'],['modelo','Modelo'],['categoria','Categoría'],['descripcion','Descripción'],['precio_compra','Precio compra'],['precio_venta','Precio venta'],['unidad','Unidad']];
function isSupplier(x){return /PROVEEDOR/i.test(String(x.datos_extra?.tipo||''))}
function applySourcePrice(x){const supplier=isSupplier(x);const source=Number(x.datos_extra?.precio_fuente||0);if(source>0){if(supplier&&!Number(x.precio_compra||0))x.precio_compra=source;if(!supplier&&!Number(x.precio_venta||0))x.precio_venta=source}return x}
async function openEditor(id){current=id;const u=await uid();const r=await supabaseClient.from('catalogo_items').select('id,codigo,nombre,marca,modelo,categoria,descripcion,precio_compra,precio_venta,unidad,datos_extra').eq('catalogo_id',id).eq('user_id',u);if(r.error)throw r.error;items=(r.data||[]).map(applySourcePrice);renderEditor()}
function renderEditor(){let m=document.querySelector('#catEditorModal');if(!m){m=document.createElement('div');m.id='catEditorModal';m.className='cat-editor-backdrop';document.body.appendChild(m)}m.innerHTML=`<div class="cat-editor"><header><div><span class="eyebrow">REVISIÓN ANTES DE INVENTARIO</span><h2>Editar productos del catálogo</h2><p>Corrige nombres, códigos, modelos y precios. Nada se enviará a Productos/Inventario hasta que tú lo confirmes.</p></div><button type="button" class="cat-editor-close">×</button></header><div class="cat-editor-tools"><input id="catEditorSearch" placeholder="Buscar código, nombre o modelo…"><span id="catEditorCount">${items.length} productos</span><button id="catEditorSave" class="btn btn-primary">Guardar cambios</button><button id="catEditorSend" class="btn btn-secondary">Pasar seleccionados a Inventario</button></div><div id="catEditorStatus" class="cat-editor-status" role="status"></div><div id="catEditorRows"></div></div>`;m.querySelector('.cat-editor-close').onclick=closeEditor;m.querySelector('#catEditorSearch').oninput=e=>drawRows(e.target.value);m.querySelector('#catEditorSave').onclick=()=>saveAll().catch(e=>setStatus('Error al guardar: '+e.message,'error'));m.querySelector('#catEditorSend').onclick=async e=>{const b=e.currentTarget;if(b.disabled)return;b.disabled=true;b.textContent='Preparando…';try{await sendSelected(b)}catch(err){setStatus('❌ '+(err.message||err),'error');alert('No se pudo completar la importación.\n\n'+(err.message||err))}finally{if(document.body.contains(b)){b.disabled=false;b.textContent='Pasar seleccionados a Inventario'}}};drawRows('')}
function setStatus(text,type='info'){const e=document.querySelector('#catEditorStatus');if(e){e.textContent=text;e.className=`cat-editor-status ${type}`}}
function drawRows(q){const box=document.querySelector('#catEditorRows');if(!box)return;const z=String(q||'').toLowerCase();const shown=items.filter(x=>!z||[x.codigo,x.nombre,x.marca,x.modelo,x.categoria].some(v=>String(v||'').toLowerCase().includes(z)));box.innerHTML=shown.map(x=>{const supplier=isSupplier(x);const sourcePrice=Number(x.datos_extra?.precio_fuente||0);const suspicious=!String(x.nombre||'').trim()||/^Producto para revisar$/i.test(String(x.nombre||''))||Number(supplier?x.precio_compra:x.precio_venta||sourcePrice||0)<=0;return `<article class="cat-editor-row ${suspicious?'cat-editor-suspicious':''}" data-id="${x.id}"><label class="cat-editor-check"><input type="checkbox" class="cat-select" checked> <span>Importar</span></label><div class="cat-editor-origin ${supplier?'supplier':'own'}">${supplier?'PROVEEDOR · PRECIO DE COMPRA':'PROPIO · PRECIO DE VENTA'}</div><div class="cat-editor-grid">${fields.map(([k,l])=>{const label=supplier&&k==='precio_compra'?'Precio compra (del proveedor)':!supplier&&k==='precio_venta'?'Precio venta (del catálogo)':l;return `<label class="cat-field ${k==='descripcion'?'wide':''}"><span>${label}</span><input data-k="${k}" value="${esc(x[k]??'')}"></label>`}).join('')}<div class="cat-source"><span>Página ${esc(x.datos_extra?.pagina||'?')}</span><span>Fuente: ${esc(x.datos_extra?.confianza||'PDF')}</span><span>Precio fuente: S/ ${sourcePrice.toFixed(2)}</span>${sourcePrice>0&&supplier?'<strong>✓ Precio fuente asignado a compra</strong>':''}${suspicious?'<strong>⚠ Revisar antes de importar</strong>':''}</div></div><button type="button" class="cat-editor-delete">Eliminar</button></article>`}).join('')||'<div class="cat-editor-empty">No hay productos que coincidan.</div>';box.querySelectorAll('.cat-editor-delete').forEach(b=>b.onclick=async()=>{const row=b.closest('.cat-editor-row');const id=row.dataset.id;if(!confirm('¿Eliminar este producto detectado del catálogo?'))return;const u=await uid();const r=await supabaseClient.from('catalogo_items').delete().eq('id',id).eq('user_id',u);if(r.error){alert('No se pudo eliminar: '+r.error.message);return}items=items.filter(x=>String(x.id)!==String(id));drawRows(document.querySelector('#catEditorSearch')?.value||'')})}
function collectRows(rows){for(const row of rows){const x=items.find(v=>String(v.id)===String(row.dataset.id));if(!x)continue;row.querySelectorAll('[data-k]').forEach(i=>{const k=i.dataset.k;x[k]=(k.startsWith('precio_')?Number(String(i.value||'0').replace(',','.')):i.value.trim())});applySourcePrice(x)}}
function collect(){collectRows([...document.querySelectorAll('.cat-editor-row')])}
async function saveOne(x,u){applySourcePrice(x);if(!String(x.codigo||'').trim()||!String(x.nombre||'').trim())return;const r=await supabaseClient.from('catalogo_items').update({codigo:x.codigo||null,nombre:x.nombre||'Producto sin nombre',marca:x.marca||null,modelo:x.modelo||null,categoria:x.categoria||'Accesorios',descripcion:x.descripcion||null,precio_compra:Number(x.precio_compra||0),precio_venta:Number(x.precio_venta||0),unidad:x.unidad||'UND'}).eq('id',x.id).eq('user_id',u);if(r.error)throw new Error(`${x.codigo}: ${r.error.message}`)}
async function saveAll(silent=false){collect();const u=await uid();const errors=[];for(const x of items){try{await saveOne(x,u)}catch(e){errors.push(e.message)}}if(errors.length)throw new Error(errors.slice(0,3).join(' | '));if(!silent)setStatus('✓ Cambios guardados correctamente.','success')}
async function sendSelected(button){const visible=[...document.querySelectorAll('.cat-editor-row')];collectRows(visible);const selected=visible.filter(r=>r.querySelector('.cat-select')?.checked).map(r=>items.find(x=>String(x.id)===String(r.dataset.id))).filter(Boolean);if(!selected.length)throw new Error('Selecciona al menos un producto.');const u=await uid();setStatus(`Guardando ${selected.length} productos seleccionados…`,'info');for(let i=0;i<selected.length;i++){await saveOne(selected[i],u);setStatus(`Guardando cambios: ${i+1}/${selected.length}…`,'info')}setStatus(`Enviando ${selected.length} productos a Productos…`,'info');const p=await supabaseClient.from('productos').select('id,codigo,precio_compra,precio_venta,descripcion,stock,activo').eq('user_id',u);if(p.error)throw p.error;const map=new Map((p.data||[]).filter(x=>x.codigo).map(x=>[String(x.codigo).trim().toUpperCase(),x]));let created=0,updated=0,skipped=0;const errors=[];for(let i=0;i<selected.length;i++){const x=selected[i];const codigo=String(x.codigo||'').trim().toUpperCase();const nombre=String(x.nombre||'').trim();setStatus(`Importando ${i+1}/${selected.length}: ${codigo||'sin código'}…`,'info');if(!codigo||!nombre||/^Producto para revisar$/i.test(nombre)){skipped++;continue}const supplier=isSupplier(x);const sourcePrice=Number(x.datos_extra?.precio_fuente||0);const compra=supplier?(Number(x.precio_compra||0)>0?Number(x.precio_compra):sourcePrice):Number(x.precio_compra||0);const venta=supplier?Number(x.precio_venta||0):(Number(x.precio_venta||0)>0?Number(x.precio_venta):sourcePrice);const marker=supplier?'[MARC-PROVEEDOR]':'[MARC-PROPIO]';const clean=String(x.descripcion||'').replace(/^\[MARC-(?:PROVEEDOR|PROPIO)\]\s*/i,'');const payload={nombre,marca:x.marca||null,modelo:x.modelo||null,categoria:x.categoria||'Accesorios',descripcion:`${marker} ${clean}`.trim(),unidad:x.unidad||'UND',precio_compra:compra,precio_venta:venta,activo:true,updated_at:new Date().toISOString()};try{const old=map.get(codigo);if(old){const r=await supabaseClient.from('productos').update(payload).eq('id',old.id).eq('user_id',u);if(r.error)throw r.error;updated++}else{const r=await supabaseClient.from('productos').insert({user_id:u,codigo,...payload,stock:0,stock_minimo:0}).select('id,codigo').single();if(r.error)throw r.error;created++;map.set(codigo,r.data)}}catch(e){errors.push(`${codigo}: ${e.message||e}`)}}if(errors.length){setStatus(`⚠ Terminó con ${errors.length} errores. Nuevos: ${created} · Actualizados: ${updated} · Omitidos: ${skipped}`,'error');alert(`Importación terminada con observaciones.\n\nNuevos: ${created}\nActualizados: ${updated}\nOmitidos: ${skipped}\nErrores: ${errors.length}\n\n${errors.slice(0,8).join('\n')}`)}else{setStatus(`✓ Importación completa: ${created} nuevos · ${updated} actualizados · ${skipped} omitidos.`,'success');alert(`✓ Productos enviados a Productos/Inventario.\n\nNuevos: ${created}\nActualizados: ${updated}\nOmitidos: ${skipped}`)}if(created+updated>0){setTimeout(()=>{closeEditor();if(typeof window.render==='function')window.render('inventory')},300)}}
function closeEditor(){document.querySelector('#catEditorModal')?.remove();current=null;items=[]}
function enhance(){const list=document.querySelector('#cat4List');if(!list)return;list.querySelectorAll('.cat4-row').forEach(row=>{if(row.querySelector('.cat4-edit'))return;const del=row.querySelector('.cat4-delete'),small=row.querySelector('.cat4-main small');if(!del||!/Procesado/i.test(small?.textContent||''))return;const b=document.createElement('button');b.type='button';b.className='btn btn-secondary cat4-edit';b.textContent='Revisar productos';b.onclick=()=>openEditor(del.dataset.id).catch(e=>alert(e.message||e));del.before(b)})}
const start=()=>{enhance();new MutationObserver(enhance).observe(document.body,{childList:true,subtree:true})};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else setTimeout(start,600);window.MARC_OPEN_CATALOG_EDITOR=openEditor;
})();
